using UnityEngine;
using System.Collections;
using System.Collections.Generic;

public class PathFinder : MonoBehaviour
{
    List<PathNode> FindPath(PathNode start, PathNode end, PathNode[,] grid)
    {
        List<PathNode> path = new List<PathNode>();

        return path;
    }
}
